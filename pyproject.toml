[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "synapse-nn"
version = "0.1.0"
description = "A pure Python neural network framework built from scratch."
readme = "README.md"
requires-python = ">=3.10"

authors = [
  {name = "Yashdeep"}
]